<%= form_for(@firework_comment) do |f| %>
  <% if @firework_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@firework_comment.errors.count, "error") %> prohibited this firework_comment from being saved:</h2>

      <ul>
      <% @firework_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
