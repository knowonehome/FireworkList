<%= form_for(@firework) do |f| %>
  <% if @firework.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@firework.errors.count, "error") %> prohibited this firework from being saved:</h2>

      <ul>
      <% @firework.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :firework_name %><br>
    <%= f.text_field :firework_name %>
  </div>
  <div class="field">
    <%= f.label :style %><br>
    <%= f.text_field :style %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit "Update Firework", class: 'btn btn-sm btn-info' %>
  </div>
<% end %>
